numbers = list(map(int, input("Enter numbers: ").split()))

unique_numbers = set(numbers)

if len(unique_numbers) < 2:
    print(-1)
else:
    sorted_numbers = sorted(unique_numbers)
    print("Second highest number:", sorted_numbers[-2])
